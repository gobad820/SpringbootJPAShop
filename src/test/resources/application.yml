spring: # 설정이 없으면 스프링이 자동으로 메모리모드로 돌려야한다.
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 동작하게 한다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
    org.hibernate.orm.jdbc.bind: trace
